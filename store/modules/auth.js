// types

// initial state
const initialState = {}

// reducer
const AuthReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

//actions

// thunks
